# 🐛 Suivi des Bugs (Bug Tracking)

| ID | Statut | Date | US | Description | Sévérité | Fix Appliqué |
|---|---|---|---|---|---|---|
| `B1A2C3` | 🟢 Résolu | 2026-04-16 | Navigation | Redirection incorrecte lors de la création d'un nouvel espace (fall-back localStorage) | Moyenne | Désactivation du fallback localStorage si `new=true` est présent dans l'URL. |
| `D4E5F6` | 🟢 Résolu | 2026-04-16 | Catalogue | Absence de bouton de suppression groupée pour les actions locales | Basse | Ajout d'un bouton `Trash2` animé dans la toolbar du catalogue avec confirmation native. |
| `E7F8G9` | 🟢 Résolu | 2026-04-16 | Layout | Erreur TS: `className` non défini sur le composant `TopBar` | Basse | Ajout de `className` et `subtitle` dans l'interface `TopBarProps` et implémentation dans le composant. |
| `H0I1J2` | 🟢 Résolu | 2026-04-16 | UI | Erreur TS: Taille `"xs"` non supportée par le composant `Button` | Basse | Remplacement de `"xs"` par `"sm"` dans `LocalActionEditModal.tsx`. |
| `K3L4M5` | 🟢 Résolu | 2026-04-16 | Catalogue | Erreur TS: Comparaison invalide type `Category` vs `string` | Moyenne | Correction de la comparaison dans `LocalList.tsx` pour utiliser `.name` si c'est un objet. |
| `B001A1` | 🟢 Résolu | 2026-05-10 | Instance | Erreur `Unique constraint failed` (id) lors de la création d'espace (séquence PG décalée). | Majeure | Synchronisation globale des séquences PG via script `fix-sequences.ts`. |
| `B002A2` | 🟢 Résolu | 2026-05-10 | Instance | `syncPeriods` échoue silencieusement si `gameEndDate` est nul, bloquant les imports CSV. | Majeure | Ajout d'un log d'avertissement explicite et prévention du retour silencieux. |
| `B003A3` | 🟢 Résolu | 2026-05-10 | Instance | À l'ouverture d'un espace, la période de la date courante ne s'ouvre pas automatiquement. | Majeure | (1) Suppression du filtre `schoolYear` dans `handleCurrentPeriodActivation` (compatibilité legacy). (2) Ajout de l'appel à `handleCurrentPeriodActivation` dans `create()` après `syncPeriods`. |
| `B31400` | 🟢 Résolu | 2026-05-11 | Refonte InstanceYear | **API Périodes** : Erreur 400 sur `GET /periods` lors de la création d'un espace. | Bloquant | Extraction du bloc Périodes dans un nouvel onglet dédié pour éviter l'appel prématuré et utilisation stricte de `instanceYearId`. |
| `B99ALL` | 🟢 Résolu | 2026-05-11 | Refonte InstanceYear | **Fuite d'années** : Une instance s'affiche sur toutes les années même sans `InstanceYear` actif. | Majeure | Ajout d'une condition `some: { schoolYear: sy }` dans `InstanceService.findAll` pour filtrer proprement. |
| `C001EQ` | 🟢 Résolu | 2026-05-11 | Admin / InstanceYear | **Équipes multi-années** : Les équipes importées s'affichent sur toutes les années scolaires alors qu'elles devraient être cloisonnées à leur `instanceYearId`. Problème probable dans `team.service.findAll` ou dans la query de teams passée au dashboard. | Majeure | Suppression de `instanceYearId` de la requête frontend `fetchTeams` ; le backend (TeamController) résout désormais dynamiquement et correctement l'ID via le `schoolYear`. |
| `C002LK` | 🟢 Résolu | 2026-05-11 | Admin / Dashboard | **Cadenas incohérent** : Le bouton cadenas (toggle `isOpen`) sur la page dashboard ne passe pas l'année scolaire active (`schoolYear`) dans le body du PATCH. La bascule ouvre/ferme probablement la mauvaise `InstanceYear` ou agit sans contexte d'année. | Majeure | Ajout de `schoolYear` dans le body de la requête PATCH dans `toggleInstanceStatus`. |
| `C003SC` | 🟢 Résolu | 2026-05-11 | Jeu v1 / Scores | **Page Scores vide** : Les colonnes "Score Semaine" et "Score Total" n'affichent aucun résultat. Le composant `ScoresSemaine` appelle `/teams/total?week_id=...` via legacy-api mais la requête dans `getTeamsTotal` filtre les `actionsDone` par `period.instanceYearId` en condition imbriquée potentiellement invalide. | Majeure | Correction de la requête Prisma dans `getTeamsTotal` en extrayant d'abord les IDs de périodes (array `periodIds`) puis en filtrant via `where: { periodId: { in: periodIds } }`. |
| `C004CO` | 🟢 Résolu | 2026-05-11 | Jeu v1 / Impacts | **Format décimales** : Sur la page "Economies réalisées", les valeurs CO2 sont affichées directement (`impacts.scoreglobal + " tCO2e"`) sans formatage à 1 décimale. Les déchets et l'eau ont déjà une fonction `ajusteUnitePoids`/`ajusteUniteVolumeEau` mais l'arrondi doit être fixé à 1 seule décimale (actuellement `toFixed(1)` OK pour la conversion mais la valeur brute en kg affiche trop de décimales). | Mineur | Modification de `ajusteUnitePoids` et `ajusteUniteVolumeEau` pour formater avec `Number().toFixed(1)` même quand la valeur est inférieure à 1000. |
| `C005UN` | 🟢 Résolu | 2026-05-11 | Jeu v1 / Impacts | **Unités CO2** : Les valeurs CO2 sont toujours affichées en tCO2e sans conversion kg/t. Il faut appliquer la même logique que `ajusteUnitePoids` : en dessous de 1t → afficher en kg, au-dessus → en tonne avec 1 décimale. | Mineur | Création et application de la fonction `ajusteUniteCO2` sur la page Impacts, qui bascule dynamiquement entre kgCO2e et tCO2e. |
| `C006FT` | 🟢 Résolu | 2026-05-11 | Jeu v1 / Page Moi | **Débordement police** : Sur la page "Moi", la ChildCard utilise des classes Tailwind (`text-sm`, `md:text-xl`, `font-extrabold`) mais la police du jeu override ces styles. Le texte déborde de la carte sur mobile. Problème de conflit entre la police globale du jeu et les utilitaires Tailwind. | Mineur | Remplacement des classes Tailwind en conflit par les classes CSS natives (`titre`, `bold-counter`) dans `ChildCard.jsx`. |
| `C007TU` | 🟢 Résolu | 2026-05-13 | Admin / Équipes | **Modification équipe bloquée** : Le bouton "Enregistrer les modifications" du `TeamEditModal` ne fonctionne pas. Le PATCH `/teams/:id` envoie `instanceId` et `schoolYear` dans le body mais ces champs ne font pas partie des colonnes de l'entité `Team` — Prisma rejette la requête. De plus, `@IsHexColor` dans `CreateTeamDto` rejette les couleurs pastel (ex: `#D1FAE5`). | Majeure | (1) Suppression de `@IsHexColor` et passage de `instanceId` en `@IsOptional` dans `CreateTeamDto`. (2) Ajout de `instanceYearId` comme champ optionnel. (3) Extraction des champs `{ name, color, icon }` uniquement dans `team.service.update()` avant le `prisma.team.update()`. |
| `C008IC` | 🟢 Résolu | 2026-05-13 | Admin / Équipes | **Icône non sauvegardée** : Le `TeamEditModal` sauvegarde l'icône sous forme de chaîne courte (ex: `'leaf'`, `'globe'`) mais le jeu tente de charger `teams/leaf` via l'URL des assets, qui n'existe pas dans `/uploads/teams`. L'icône n'est jamais affichée dans le jeu et se perd silencieusement. | Majeure | Refonte du `TeamEditModal` : l'icône est désormais un nom de fichier réel (ex: `Chat.png`) provenant de la galerie `/teams/icons` ou d'un upload. |
| `C009UP` | 🟢 Résolu | 2026-05-13 | Admin / Équipes | **Pas d'upload d'icône** : L'interface `TeamEditModal` ne propose que des icônes SVG Lucide prédéfinies, sans aucun bouton d'upload de fichier image. Aucun endpoint `POST /teams/upload-icon` n'existe côté backend. L'admin ne peut pas associer une vraie image PNG/JPG aux équipes (alors que `/uploads/teams/` contient déjà 30+ images existantes). | Majeure | Ajout de `POST /teams/upload-icon` (Multer, diskStorage) et `GET /teams/icons` côté backend. Ajout d'un bouton Upload + galerie de sélection dans `TeamEditModal`. |
| `C010CO` | 🟢 Résolu | 2026-05-13 | Admin / Équipes | **Couleur d'équipe non personnalisable** : Le `TeamEditModal` n'offre que 6 couleurs pastel prédéfinies en dur. Aucun color-picker natif (`<input type="color">`) n'est disponible. L'admin ne peut pas définir la couleur d'accent précise de son équipe. | Mineur | Ajout d'un `<input type="color">` natif à côté des 12 couleurs prédéfinies. Affiche la valeur hex courante. |
| `C011UP` | 🟢 Résolu | 2026-05-13 | Admin / Équipes | **Upload d'icône non fonctionnel / UI incohérente** : L'upload d'icône d'équipe ne fonctionne pas et l'UX diffère de celle de l'upload d'avatar (où on clique directement sur l'image). | Majeure | Suppression du bouton séparé et transformation de l'encart d'icône en zone cliquable `group cursor-pointer` qui déclenche l'input file caché. Ajout d'un overlay au hover pour indiquer l'action d'upload. |
| `C012AM` | 🟢 Résolu | 2026-05-13 | Admin / Dashboard | **Changement de référent (AM) cassé** : La réassignation d'un gestionnaire/référent à une instance ne fonctionne plus. | Majeure | (1) Le frontend `handleAdminChange` n'envoyait pas `schoolYear` dans le PATCH → le backend ciblait '2024-2025' par défaut. Ajout de `schoolYear` au body. (2) `adminId` était extrait du `updateData` et ne mettait plus à jour `Instance.adminId` — réinjection dans `updateData`. |
| `C013IY` | 🟢 Résolu | 2026-05-13 | Admin / InstanceYear | **Création multi-années non désirée** : La création d'un nouvel espace crée une instance d'école et des `InstanceYear` pour toutes les années au lieu de se limiter à l'année courante/sélectionnée. | Majeure | (1) Suppression de l'auto-création dans `instance.service.update()` — si l'`InstanceYear` n'existe pas, seule l'`Instance` est mise à jour. (2) Suppression de l'auto-création dans `year.service.resolveInstanceYear()` — retourne `null` si inexistant. (3) Le hook `useInstanceYear` gère désormais `null` sans crash. |
| `C014AC` | 🟢 Résolu | 2026-05-13 | Admin / Dashboard | **Auto-complétion école cassée** : Lors de la navigation vers la création d'un nouvel espace depuis un espace existant, l'auto-complétion ne fonctionne plus et le formulaire garde les anciennes données. | Majeure | (1) Le paramètre `new=true` ne réinitialisait pas l'`instanceId` à `null` dans `OrganizationPage` si on y accédait sans recharger la page. (2) Ajout du reset explicite du formulaire dans `GeneralSettings` quand `currentInstance` devient `null`. |
| `C015PE` | 🟢 Résolu | 2026-06-10 | Jeu / Périodes | **Actions done non réinitialisées** : Lorsqu'une nouvelle période débute, les actions réalisées de la période précédente restent sélectionnées. | Majeure | (1) backend-v2: Ajout d'auto-correction sur `getWeek` via `getOpenPeriod` et valeur par défaut de période sur `getActionsDone`. (2) sosplanete-v1: Ajout de `currentWeek?.id` (et `childId`) dans le tableau de dépendances des hooks `useEffect` des pages Actions, Fiche, Categorie, Moi et Scores. |


